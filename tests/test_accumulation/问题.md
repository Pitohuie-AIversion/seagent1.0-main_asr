# 累计集成测试问题登记

## 2026-07-28 真实模型基线

- 模型：本地 Qwen3.5-9B，非 `OFFLINE_MOCK`。
- 单元测试：464/464 通过。
- 累计集成测试：8/23 通过；该结果包含测试数据漂移造成的误报。

## 仍需保留的真实回归

- TS-01：明确输入“海底油气管道”时，真实模型未稳定提取 `cable_type`。
- TS-17：口语化“采油树插拔”未稳定映射到 `tree_valve_operation`。
- TS-18：混合域内、域外多任务时，回复未明确拒绝域外部分。
- TS-19：同轮提出两个域内任务时，回复未稳定要求用户选择一个任务。

## 测试维护规则

- 不得通过删除业务断言或扩大无意义关键词集合来掩盖真实回归。
- 每个约束场景必须使用不会触发其他地理约束的基线坐标。
- 正常遥测由统一工厂生成，每个场景只覆盖目标异常字段。
- 自动化运行使用服务端当前模拟时间；固定时间只允许出现在显式设置模拟时间的手工文档中。
- 当前只能匹配回复语义；后端提供结构化 `violation_codes` 后，应迁移为约束代码断言。

## 本轮已修正的测试漂移

- 原正常坐标 `(19.8,113.5)` 位于 DVL 风险区，已换成安全基线。
- TS-02 已切换到真实禁入区坐标；TS-13 已使用陵水17-2真实经度和当前履带式设备。
- 端口、装备型号、单机编号、工具名和遥测时间基线已同步。

context 在我测试的过程中，后端一开始是输入的流速超限的state，模型能够正常识别超限并按照我脚本内的逻辑给我发出提醒，这是正确的。但是在我更新完后端信息，即流速正常之后，我再次询问现在的情况，流速信息还停留在超限的数值（网页的回答中），但是我可以发现的是state.yaml里的流速信息已经做了更新，但是模型似乎没有用更新后的state.yaml里的内容再做判断。帮我找到这个bug存在的原因