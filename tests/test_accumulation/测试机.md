为了方便您和同事在前端 HUD 页面上以当前时间（~14:28）完整复现“**物理状态超标拦截 ➔ 后台参数更新 ➔ 发送重新检查通过**”的完整闭环，请对照以下步骤和对话文本进行操作：

---

### 第一阶段：流速超标，任务被硬拦截

#### 1. 【终端准备】设置流速超标（注入 1.5 m/s）
在终端中执行以下命令，将工作级 ROV `sealien_work_class` 的实时流速设定为不可作业的 **1.5 m/s**，更新时间设定为 **14:20**（保证不超过当前时间 1 小时）：
```bash
curl -X POST http://localhost:8890/api/robot/set-state-info \
  -H "Content-Type: application/json" \
  -d '{"robot_name":"sealien_work_class","params":{"current_velocity":1.5,"update_timestamp":"2026-07-10T14:20:00+08:00","overall_status":"available","survival_status":"normal","thruster_status":"normal","depth_keeping_status":"normal","sonar_status":"normal","vision_status":"normal","arm_status":"normal","end_effector_status":"normal","acoustic_comms_status":"normal","tether_connection_status":"normal"}}'
```

#### 2. 【前端对话】发送任务描述
进入前端网页，点击 **“重新开始”**（以开启干净的新会话），然后在聊天框中输入并发送以下内容：
> 采油树控制面板插入，开始时间现在，结束时间五小时后，水深800米，油田名称流花11-1，油田经纬度(19.9,113.6)，井口编号A03，设备类型工作级ROV，设备名称sealien_work_class，携带工具：液压扭矩工具、机械臂，支持船：海洋石油681

#### 3. 【预期回复】
大模型会返回红色/黄色的警告信息，明确拒绝执行：
> ⛔ **当前违规详情：流速状态监测 - 禁止**
> 当前流速为 **1.50 m/s**，已超过安全上限（禁止级）。在强流环境下，ROV 极易失控，系统已自动禁止执行。请您等待海况改善后重新发起任务，或修改任务时间。

---

### 第二阶段：海况转好，后台更新参数

#### 4. 【终端操作】更新流速为安全值（注入 0.3 m/s）
此时，模拟海况好转，在终端中执行以下命令，将流速修改为安全的 **0.3 m/s**，更新时间设定为 **14:25**（保证最新）：
```bash
curl -X POST http://localhost:8890/api/robot/set-state-info \
  -H "Content-Type: application/json" \
  -d '{"robot_name":"sealien_work_class","params":{"current_velocity":0.3,"update_timestamp":"2026-07-10T14:25:00+08:00","overall_status":"available","survival_status":"normal","thruster_status":"normal","depth_keeping_status":"normal","sonar_status":"normal","vision_status":"normal","arm_status":"normal","end_effector_status":"normal","acoustic_comms_status":"normal","tether_connection_status":"normal"}}'
```

---

### 第三阶段：前端重新检查，解锁任务

#### 5. 【前端对话】发送重新校验请求
在刚才同一个聊天窗口中，不需要重新发一大段话，直接输入并发送：
> 重新检查

#### 6. 【预期回复】
大模型会重新加载校验逻辑，检测到 `state.yaml` 中的流速已降为 `0.3 m/s`，给出绿色的通过结论：
> 收到，已重新校验。
> 
> **【重新校验结果】环境安全 ✅**
> * **流速监测**：0.3 m/s（正常 ✅）
> 
> 此前检测到的流速禁止异常已消除，当前参数已满足所有安全约束。
> 请确认是否以上述参数正式下达该任务？（回复“确认”或“是”以生成任务工单）